#import "../../config/acronyms.typ": *
#import "../../config/functions.typ": *
#include "../../config/config.typ"

== Übernahme in Desigo CC<sec:uebernahme>

#kommentar[Kurz halten, aber nicht weglassen. Der #acro("PDE") nennt Desigo CC nicht als Zielapplikation (siehe @sec:pde_ziel), weshalb der gelungene Import selbst ein Ergebnis der Arbeit ist. Festzuhalten sind der Weg des Imports, die Bedingung an den Adressversatz auf beiden Seiten sowie das Anlegen der Instanzen je Unit Identifier.]
