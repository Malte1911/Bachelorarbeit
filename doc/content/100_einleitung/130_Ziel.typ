#import "../../config/acronyms.typ": *
#import "../../config/functions.typ": *
#include "../../config/config.typ"

== Zielsetzung und Abgrenzung<sec:ziel>

#kommentar[Ziel aus der Anmeldung uebernehmen. Danach die Abgrenzungen buendeln, die bisher verstreut in Kapitel 3 stehen: Informationssicherheit nicht im Scope (siehe @sec:stakeholder), SENTRON Powerconfig wird nicht abgeloest (siehe @sec:konzept), Schutzfunktion der Geraete nicht Gegenstand (siehe @sec:systemanalyse), eigenes Objektmodell fuer das Powercenter nicht gefordert.]
